import std.io

def fib : int
    @n : int

    if n == 0:
        return 1
    elif n == 1:
        return 1
    else:
        return fib(n - 1) + fib(n - 2)

def main : void
    n = read_int('Enter a number: ')
    print_int(fib(n))
